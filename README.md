# AETHER — Autonomous AI Crypto Trading Agent
Built with @grok — Solana | LangChain | GPT-4o | Jupiter

## Run in 60 seconds
```bash
pip install -r requirements.txt
cp config.example.yaml config.yaml
python main.py --test
